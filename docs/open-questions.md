# Open Questions & Ambiguous Requirements

Status: **RESOLVED** (Q1–Q10 on 2026-09-03, Q11–Q34 on 2026-09-05) — all items below accepted as the stated recommendation, confirmed by the project owner in lieu of a full team meeting. Kept as the running log per the "How to Resolve These" section; revisit any individual item if it turns out wrong once the relevant phase is actually built.

| # | Question | Where it matters | Decided |
|---|---|---|---|
| **Q1** | Can one user hold multiple roles at the same organization (e.g., Recruiter + Hiring Manager)? | `authorization.md`, `database.md` (`UserOrganizationRole`) | **Decided:** Yes — allow multiple roles per user per org via the many-to-many join table already modeled. |
| **Q2** | Must a candidate verify their email before applying to jobs? | REQ-AUTH-001, REQ-APP-001, `authentication.md` §3 | **Decided:** Not gated on verification for MVP; verification is required before offer acceptance. **Doc/code mismatch found 2026-09-05 (building the F frontend milestones' forgot/reset-password pages):** `OffersService.respond()` (M10) has no `emailVerified` check at all -- an unverified candidate can accept an offer today. Flagging per CLAUDE.md rule 9 rather than silently adding the check now (a real behavior change, not a frontend concern) or silently leaving the doc wrong. Revisit: either add the check to `OffersService.respond()` to match this decision, or update this row if the "before offer acceptance" gate was never actually meant to be enforced. |
| **Q3** | Is a "staff" account (Recruiter, HR, etc.) the same `User` login as a candidate account, or must they be separate identities? | REQ-AUTH-008, `database.md` (`User`/`UserOrganizationRole`) | **Decided:** Same identity — one login, org-scoped roles determine capability. |
| **Q4** | When a recruiter rejects a candidate at screening, is the candidate notified automatically, silently, or is it org-configurable? | REQ-APP-002/003, `Notifications` module | **Decided:** Org-configurable, per-org boolean setting, default ON. |
| **Q5** | Is interview evaluation numeric scoring, qualitative feedback, or both? | REQ-EVAL-001, `database.md` (`Evaluation.scores`, `.comment`, `.recommendation`) | **Decided:** Both — structured per-competency score (1-5) + free text + an overall recommendation enum. |
| **Q6** | Who makes the final hiring decision — Hiring Manager only, or also Recruiter? | REQ-HIRE-001, `authorization.md` §3 | **Decided:** Hiring Manager is the primary decision-maker; Recruiter can propose/advance to the decision stage but not finalize. |
| **Q7** | If a candidate updates their "primary" CV after applying somewhere, should past applications keep showing the CV version as it was at application time, or always the candidate's current CV? | `database.md` §3 | **Decided:** Snapshot-at-application-time via `Application.cvId`. |
| **Q8** | File storage: AWS S3 or Cloudinary? | `decisions/ADR-004-file-storage-provider.md`, `deployment.md` §4 | **Decided:** Cloudinary for MVP. |
| **Q9** | Frontend hosting: Vercel vs. containerized alongside backend? | `deployment.md` §4 | **Decided:** Containerized alongside the backend via the dev/prod Docker Compose stack (consistent with `deployment.md` §2's docker-compose design); revisit if a hosting constraint later favors Vercel. |
| **Q10** | Is Subscriptions/Payments in scope for the MVP demo, or can it be stubbed (single free plan, no real Stripe checkout flow) until later? | `team-plan.md` §7 (MVP scope) | **Decided:** Stubbed for MVP — single free plan, no real Stripe checkout flow until M14. |
| **Q11** | `authorization.md` §2 states "every role except SUPER_ADMIN is organization-scoped" (assigned via `UserOrganizationRole`), but a Candidate applies across any number of organizations without being a member of any of them — the schema has no way to give a Candidate an org-scoped role, and none of the candidate-owned tables (`CandidateProfile`, `Application`, etc.) are org-scoped. Documentation bug, flagged per `CLAUDE.md`'s doc-conflict rule rather than picked silently. | `authorization.md` §2, `database.md` (`User`/`UserOrganizationRole`), M3/M4 (Auth/RBAC) | **Decided:** CANDIDATE is implicit for any authenticated `User` — no `UserOrganizationRole` row is ever created for it. The seeded `CANDIDATE` `Role`/`Permission` rows (M2.3) remain catalog/documentation data only. Candidate-owned actions are authorized via ownership checks (`userId` matches the resource), not a role lookup — the same pattern `SUPER_ADMIN` already uses via `User.isSuperAdmin` instead of a role row. |
| **Q12** | No email/SMTP provider has been chosen (no ADR exists), but `authentication.md` requires emailing verification and password-reset links starting in M3. | `authentication.md` §3, `security.md` §9 (SMTP credentials) | **Decided:** Stub for now — verification/reset links are logged via the server logger, not actually emailed, until the team picks a provider. Real email delivery is a separate future ticket, not a blocker for M3. |
| **Q13** | No document specifies how the *first* Super Admin account is created — every registration flow (candidate, org owner) creates a `User` with `isSuperAdmin=false`, and there is deliberately no API to set it (exposing that would be a privilege-escalation hole). Without one, REQ-AUTH-003 (org approval) has no actor who can call it. | `authorization.md` §2/§5, REQ-AUTH-003, M2.3 (seed), M5 (Organizations) | **Decided:** Extend the existing idempotent seed script to also upsert one bootstrap Super Admin from `SUPER_ADMIN_EMAIL`/`SUPER_ADMIN_PASSWORD` env vars (dev-only placeholder values in `.env.example`, same pattern as other secrets) — not a public endpoint. |
| **Q14** | `authorization.md` §3's Super Admin permission row has `organization:approve`/`reject`/`suspend` but no `organization:read`/`list` — so `GET /api/v1/organizations` (Super Admin's paginated queue of orgs to review, REQ-AUTH-003) has no permission key to declare via `@RequirePermission()`. | `authorization.md` §3, REQ-AUTH-003, M5.2 (Organizations) | **Decided:** Gate the list endpoint under the existing `organization:approve` permission rather than inventing a new seeded permission for a single read endpoint — reviewing the queue is inherent to being able to act on it, and only `SUPER_ADMIN` holds that permission today anyway. Revisit with a dedicated `organization:read` if a future role (e.g. read-only platform support staff) needs to view the list without the approve/reject capability. |
| **Q15** | Two gaps for `GET`/`PATCH /api/v1/organizations/me`: (1) `organization:read` isn't seeded on any org-scoped role (same gap as Q14, at the org-scoped level this time), so it's unclear who besides `COMPANY_OWNER` (holder of `organization:update`) can view org settings; (2) REQ-AUTH-002 states a `PENDING_APPROVAL` org "cannot post jobs, cannot log in to org portal beyond a pending approval screen" — implying writes should be blocked pre-approval, but doesn't say so for reads or explicitly for the settings-update case. | `authorization.md` §3, REQ-AUTH-002, M5.3 (Organizations) | **Decided:** (1) No permission gate on `GET .../me` at all — any authenticated user with an org in their token context (any org-scoped role) can view their own org's name/status/rejection reason, since every staff member needs to see the "pending approval" screen, not just the Owner. (2) `PATCH .../me` (still gated by `organization:update`, i.e. Company Owner only) additionally requires `status === ACTIVE`, returning 409 otherwise — consistent with "not usable... until approved." Revisit if a future role needs write access while pending. |
| **Q16** | `database.md`'s `Invitation` model stores a raw `token` (unique, plaintext) rather than a hash, unlike every other single-use token in the schema (`RefreshToken.tokenHash`, `VerificationToken.tokenHash`) — `authentication.md` §2 documents hashing bearer tokens at rest as the established pattern precisely so a DB read/leak can't be used to redeem them directly. Documentation/schema inconsistency, flagged per `CLAUDE.md`'s doc-conflict rule. | `database.md` (`Invitation`), `authentication.md` §2, M5.4 (Organizations) | **Decided:** Follow the established hashed-token convention, not the literal schema proposal — renamed the column to `tokenHash` in a new migration. Also: REQ-AUTH-008 says an invite carries "role(s)" (plural) but `Invitation.roleId` is singular with no join table for multiple roles. Decided to keep it singular for MVP scope (no `InvitationRole` join table) — inviting someone into two roles at once is done as two separate invitations to the same email; revisit only if the frontend UX genuinely needs a single combined invite. |
| **Q17** | Two gaps found starting M6 (Jobs): (1) `authorization.md` §3 doesn't give Recruiter `job:read`, even though it grants `job:create`/`update`/`publish`/`close` — a Recruiter who can create and manage a job but supposedly can't view it makes no operational sense; (2) REQ-JOB-001 calls for an optional salary range on job creation, but `database.md`'s `Job` model proposal has no salary columns at all. Both are documentation/schema gaps, flagged per `CLAUDE.md`'s doc-conflict rule rather than picked silently. | `authorization.md` §3, REQ-JOB-001, `database.md` (`Job`), M6 (Jobs) | **Decided:** (1) Added `job:read` to Recruiter's seeded permissions and updated `authorization.md`'s table to match. (2) Added nullable `salaryMin`/`salaryMax` (`Int?`, whole-unit amounts) to the `Job` model via migration and updated `database.md` to match — no currency field exists anywhere in the schema yet, so these are unitless for now; revisit if/when multi-currency support is ever needed. |
| **Q18** | Found while implementing M6.5 (public job search), on review of `api.md` before writing it: two status-code mismatches against the already-implemented M6.3/M6.4 tickets. (1) `api.md` §1 names "publishing a job with no pipeline stages" as its literal example of a **422**, but M6.4's `publish()` had used 409 for that check. (2) `api.md` §2's worked example for `POST /jobs` says a cross-org `pipelineTemplateId` reference should be **422** ("referenced pipelineTemplateId belongs to a different org"), but M6.3's `applyTemplate()` returned 404 (following `multi-tenancy.md` §5's cross-tenant-access rule). | `api.md` §1/§2, `multi-tenancy.md` §5, M6.3/M6.4 (Jobs) | **Decided:** Fixed both to 422, matching `api.md`'s literal named examples. For (2) specifically: `multi-tenancy.md` §5's 404 rule is about *direct access to a resource by its own URL `:id`* (e.g. `GET /pipeline-templates/:id` correctly still 404s cross-tenant); this is a different situation — a foreign-org id referenced as an *input field* inside a request body for an action on the caller's own resource. Both "doesn't exist" and "exists in another org" still collapse into the same 422 response, so no existence-probing leak is introduced either way — only the status code differs from the direct-access case. |
| **Q19** | Found starting M7 (Candidate Profile & Applications): Q11 decided CANDIDATE is implicit (no `UserOrganizationRole` row) and that candidate-owned actions are "authorized via ownership checks... not a role lookup" — but `authorization.md` §3 explicitly seeds `candidateProfile:update`/`application:create`/etc. as CANDIDATE permissions, and `CLAUDE.md` rule 1 requires *every* protected endpoint to use `@RequirePermission()`. `PermissionsGuard` as built only ever read the `roles`/`isSuperAdmin` claims from the access token, neither of which can ever contain `'CANDIDATE'` for anyone (per Q11) — so no candidate-facing endpoint could ever pass `@RequirePermission()`, for *any* user, as the guard was originally written. | `authorization.md` §3, `CLAUDE.md` rule 1, Q11, Q3/REQ-AUTH-008, M7 (Candidates) | **Decided:** `PermissionsGuard` now always includes `'CANDIDATE'` in the role keys it checks, for every authenticated user, unconditionally (not just as a fallback when they hold no org roles) — per Q3/REQ-AUTH-008, the same login can be both org staff and a candidate, so a Recruiter should also pass `application:create`. This lets candidate-facing endpoints use the same centralized `@RequirePermission()` mechanism as everything else, satisfying `CLAUDE.md` rule 1 without contradicting Q11's data-model decision (still no `UserOrganizationRole` row for candidates) — the *resource-level* restriction (only your own profile/application) remains a required second layer via an ownership check in the service, the same pattern `authorization.md` §3 already prescribes for Interviewer's ownership-scoped permissions. |
| **Q20** | M7.2 (CV upload) needs real private object storage with signed, time-limited URLs (REQ-CAND-002, `security.md` §11). ADR-004 picked Cloudinary, but no Cloudinary account/API credentials exist in this dev environment (unlike other stubbed dependencies, this can't be worked around by just logging a value the way email was in Q12 — file bytes have to go *somewhere* real for upload/download to actually function). | ADR-004, `security.md` §11, REQ-CAND-002, M7.2 (Candidates) | **Decided:** Built the `StorageService` abstraction ADR-004 already calls for ("isolated behind a small internal interface... swappable later") with a `LocalStorageService` implementation backed by local disk (path from `STORAGE_LOCAL_DIR`, gitignored) instead of Cloudinary. Signed-URL semantics are real, not faked: `getSignedUrl()` HMAC-signs `key + expiry` with `STORAGE_SIGNING_SECRET` and a public `GET /api/v1/storage/:key` endpoint verifies the signature and expiry (timing-safe compare) before streaming the file — the same security property Cloudinary's signed URLs provide, just served locally. Swapping in a real `CloudinaryStorageService` later only requires a new class + one line in `StorageModule`'s provider registration; `StorageController` itself is local-storage-specific and would become dead code at that point (a real Cloudinary URL needs no local proxy). |
| **Q21** | Found starting M7.3 (job applications): `authorization.md` §3's original Candidate row had `application:create`/`withdraw` but no `application:read` — yet REQ-APP-001 explicitly says "candidate's dashboard shows the new application," which obviously requires reading it back. Same class of gap as Q17 (Recruiter missing `job:read`). | `authorization.md` §3, REQ-APP-001, M7.3 (Applications) | **Decided:** Added `application:read` to Candidate's seeded permissions and updated `authorization.md`'s table to match. The same permission key is already used by Recruiter/Hiring Manager for org-scoped reads (`authorization.md` §3) — this is intentional, not a scope leak: `@RequirePermission('application:read')` only confirms the caller may read *some* application, and the service layer's own ownership filter (`candidateId` for a candidate, `organizationId` for org staff) determines *which* ones, the same two-layer pattern already used for Interviewer's ownership-scoped `interview:read`. |
| **Q22** | Found while implementing M7.4 (screening/shortlisting): REQ-APP-002/003 says screening-pass "moves candidates into Shortlisted stage of the pipeline," but `RecruitmentStage` names are fully recruiter-defined free text per job (M6.3) — there is no canonical "Shortlisted" stage id or name to move into, and REQ-PIPE-002's fuller "move between any stages" flow names Hiring Manager as a mover too, but `authorization.md` §3 has no permission gap covering that generality for Hiring Manager, and interview/evaluation/hiring stage-moves are entirely out of scope until M8/M9. | REQ-APP-002/003, REQ-PIPE-001/002, `authorization.md` §3, M6.3 (Jobs pipeline), M7.4 (Applications) | **Decided:** Scoped M7.4 to exactly REQ-APP-002/003, gated on the existing `application:screen` permission (Recruiter only, matching REQ-APP-002's stated actor) via a single `POST .../screen` action with `decision: 'PASS' \| 'REJECT'`. PASS advances the application to whatever `RecruitmentStage` has `order = currentStage.order + 1` on that job (422 if none exists) — this satisfies "moved into Shortlisted stage" generically without assuming any stage is literally named "Shortlisted", logged to `ApplicationStageHistory` per REQ-PIPE-002's audit requirement. REJECT sets `status: REJECTED` with an optional `rejectedReason` (same field-naming precedent as `Organization.rejectedReason`) and writes an `AuditLog` row (`application.rejected`), mirroring `OrganizationsService.reject()` exactly — no stage change, since rejection is a terminal status, not a pipeline position (same reasoning M7.3 already applied to `WITHDRAWN`). The fully generic "move to any arbitrary stage, including backward, for both Recruiter and Hiring Manager" flow REQ-PIPE-002 describes is deferred to whichever milestone first needs it concretely (M8 Interviews or M9 Evaluations/Hiring) rather than built speculatively now. |
| **Q23** | Real bug found writing M7.4's e2e tests for `GET /jobs/:jobId/applications` (gated on `application:read`): an Interviewer staff member -- granted no `application:*` permission at all -- got 200, not the expected 403. Root cause is the Q19 + Q21 interaction: Q19 made `PermissionsGuard` append `'CANDIDATE'` to *every* authenticated user's role keys unconditionally, and Q21 then added `application:read` to CANDIDATE's grants -- so from that point on, *any* authenticated staff member of the correct org (regardless of their actual role's permissions) trivially satisfied `@RequirePermission('application:read')` purely via the blanket CANDIDATE fallback, on a route that is unambiguously org-resource-scoped, not candidate-self-scoped. `TenantGuard` still blocked other orgs, so this was an intra-org over-permission bug, not a cross-tenant leak -- but it silently defeated the point of gating that route on a permission at all. | `PermissionsGuard`, Q19, Q21, `authorization.md` §3, M7.4 (Applications) | **Decided:** `PermissionsGuard` now excludes the implicit `'CANDIDATE'` role key on any route that also carries `@RequireTenant()` -- that decorator only ever appears on an org-resource-scoped route (`multi-tenancy.md` §3), never a candidate-self one (`ApplicationsController`'s `/applications/*` routes have no `@RequireTenant()`), so this cleanly separates the two contexts that were sharing the `application:read` permission key without reintroducing scattered `if` checks (`CLAUDE.md` rule 1) or renaming the already-documented, multiply-used permission key. Verified no existing `@RequireTenant()` route regressed: every one of them (`JobsController`, `PipelineTemplatesController`) is gated on a `job:*`/`pipeline:manage` permission, none of which were ever in CANDIDATE's grant set, so none relied on the fallback being present. |
| **Q24** | Found starting M9 (Evaluations & Hiring): `authorization.md` §3's original Recruiter row has no `evaluation:read`, yet REQ-EVAL-002 explicitly names its actors as "Recruiter, Hiring Manager" for the aggregate evaluation view. Same class of gap as Q17/Q21/Q23. | `authorization.md` §3, REQ-EVAL-002, M9 (Evaluations) | **Decided:** Added `evaluation:read` to Recruiter's seeded permissions and updated `authorization.md`'s table to match, mirroring Q17/Q21's precedent exactly. |
| **Q25** | REQ-EVAL-001 says a submitted evaluation is "never [visible] to other interviewers on the same panel, to avoid anchoring bias, until all have submitted — configurable." No per-org settings mechanism exists anywhere in the schema yet to store such a toggle (same class of gap as Q4's rejection-notification setting, deferred for the same reason: no `OrgSettings`-shaped model exists, and building one speculatively for a single flag is premature). | REQ-EVAL-001, Q4, M9 (Evaluations) | **Decided:** For MVP, hard-code the stricter, non-configurable default: no endpoint exposes another panelist's evaluation to an interviewer at all -- an interviewer can submit their own (`POST /interviews/:interviewId/evaluation`) but there is no interviewer-facing read/list of evaluations, matching `CLAUDE.md`'s business-rule quick reference ("an interviewer... can only see interviews/candidates they're assigned to," which doesn't extend to peer evaluations). This is a strict subset of the described behavior (never, rather than "until all submitted") and satisfies the stated anti-anchoring-bias goal; the "configurable, unlocks after all submitted" nuance is deferred until a general org-settings mechanism exists for it, same as Q4's rejection-notification toggle. Recruiter/Hiring Manager's aggregate view (REQ-EVAL-002, `GET /jobs/:jobId/applications/:id/evaluations`) is unaffected -- it was never described as bias-sensitive the way peer-interviewer visibility is. |
| **Q26** | Found starting M10 (Offers): three gaps. (1) Same class as Q17/Q21/Q23/Q24 -- `authorization.md` §3 seeds neither HR Manager nor Candidate with `offer:read`, though both obviously need to view an offer (HR Manager to manage it, Candidate to decide on it) before REQ-OFFER-002's accept/decline action makes sense. (2) REQ-OFFER-001 calls for an "optional offer letter document," but `Document` (schema.prisma) only relates to `OnboardingTask` (nullable `taskId`), never to `Offer` -- no column or join exists to attach one. (3) REQ-OFFER-002's decline branch says "recruiter is notified and can reopen the pipeline," but no endpoint exists anywhere to move a `HIRED` application back to `ACTIVE` -- REQ-PIPE-002's fuller stage-movement generality was already deferred once (Q22) pending a concrete need, and "reopen after a declined offer" is exactly that kind of concrete need, just not yet built. | `authorization.md` §3, REQ-OFFER-001/002, `Document`/`Offer` schema, Q22, M10 (Offers) | **Decided:** (1) Added `offer:read` to both HR Manager's and Candidate's seeded permissions and updated `authorization.md`'s table, same fix shape as Q17/Q21/Q23/Q24. (2) Offer-letter attachment is out of scope for this ticket -- REQ-OFFER-001 itself calls it optional, and no schema relation exists to hang it off; revisit alongside a real `Offer.documentId` (or similar) column if/when the frontend needs it, rather than bolting it on speculatively now. (3) "Reopening the pipeline" after a decline is also out of scope for this ticket: `Offer.status` becomes `DECLINED` (the authoritative record of what happened to *this* offer), but `Application.status` stays `HIRED` -- a manual/future "revert to ACTIVE" action is deferred to whichever ticket first needs Q22's generic backward-stage-move capability, rather than building a one-off "un-hire" endpoint speculatively now. |
| **Q27** | REQ-DOC-001/002 and REQ-ONB-001 are explicitly marked in `requirements.md` §3.2 as "full detail will be produced at the start of their respective implementation phase" -- no main flow was ever written for them, unlike every other requirement in the doc. Starting M11, three things need deciding that the requirement text doesn't specify: (1) whether `OnboardingChecklist` creation is automatic (triggered by offer acceptance, per `database.md` §2's "created on acceptance") or an explicit HR Manager action; (2) whether a task auto-completes when a candidate uploads a document against it, or requires HR Manager review; (3) what permission gates a candidate reading their own checklist -- no seeded permission covers it (`document:read`/`onboarding:manage` are both HR-Manager-only in `authorization.md` §3, and neither is really "read the list of requested tasks" semantically). | `requirements.md` §3.2 (REQ-DOC/REQ-ONB), `database.md` §2, `authorization.md` §3, M11 (Onboarding) | **Decided:** (1) Explicit HR Manager action (`POST .../onboarding`), not automatic -- REQ-DOC-001's actor is HR Manager "requesting" documents, which reads as a deliberate step, and `database.md`'s "created on acceptance" is read as stating the *precondition* (offer must be `ACCEPTED` first) rather than mandating auto-creation; enforced as a 409 if the offer isn't `ACCEPTED` yet. (2) HR Manager review, not auto-complete -- REQ-ONB-001 names status *tracking* as an HR Manager capability, and auto-completing on upload would let an unreviewed/wrong file silently mark a task done; a separate `PATCH .../tasks/:taskId/complete` action exists for this. (3) Added a new `onboarding:read` permission (not a missing grant of an existing one, unlike Q17/Q21/Q23/Q24/Q26 -- no existing key fit) granted to Candidate only; HR Manager's existing `onboarding:manage` already covers their read access, matching the established "manage implies read+write" pattern already used for `pipeline:manage`. |
| **Q28** | Starting M12 (Notifications): REQ-NOTIF-001 says only "email/in-app notifications on key events" -- no event list, and `requirements.md` §3.2 had deferred it entirely like REQ-DOC/REQ-ONB were (Q27). Two more things need deciding: (1) `CLAUDE.md` rule 4 lists "emitted events" as the sanctioned way for a cross-cutting concern like notifications to reach across module boundaries, but the codebase's actual, already-shipped precedent for the other cross-cutting concern rule 4 names in the same breath -- `AuditLog` -- has every feature service (`OrganizationsService`, `ApplicationsService`) write to it directly via `tx.auditLog.create(...)`, not through any event/pub-sub layer, and no `@nestjs/event-emitter`-style dependency exists anywhere in the project. (2) Which specific events count as "key" enough to wire up now, given the requirement doesn't enumerate any. | `CLAUDE.md` rule 4, REQ-NOTIF-001, `requirements.md` §3.2, Q4, Q12, M12 (Notifications) | **Decided:** (1) Direct writes from the originating service (`NotificationsService.notify()` called directly, the same shape as the `AuditLog` precedent), not a new event-emitter layer -- reads rule 4's "emitted events" as describing the *conceptual* pattern (a service "emits" a cross-cutting side effect without another feature module reaching into its own tables), not literally mandating Node `EventEmitter`/pub-sub machinery this codebase has never used. Calls happen *after* the originating transaction commits, not inside it (a notification is best-effort, non-critical side data -- unlike the primary state change it doesn't need the same atomicity guarantee). "Email" is a stub, logged not sent, exactly like Q12's verification/reset emails -- no provider has been chosen. (2) Scoped to three events already flagged as needing one by earlier tickets rather than inventing a "complete" list: application rejected at screening (Q4) or at the final hiring decision (REQ-HIRE-001) -> notifies the candidate; an interview scheduled (REQ-INT-002's "interviewers are notified") -> notifies each assigned interviewer. REQ-OFFER-002's "recruiter is notified" on decline is deliberately **not** wired this round: unlike the other three, `ApplicationsService`'s existing DTOs don't expose which user should receive it (no "job owner"/"recruiter of record" concept exists -- `Job.createdById` isn't surfaced anywhere `OffersService` can reach without a new cross-module lookup), so guessing a target (all org staff? just the creator?) would be exactly the kind of silent business-rule resolution `CLAUDE.md` rule 9 warns against; revisit once that concept is decided. |
| **Q29** | Starting M13 (Subscriptions & Payments): REQ-SUB-001/REQ-PAY-001 were also deferred with no main flow (same as Q27/Q28's pattern). Two things need deciding: (1) no Stripe account/test API keys exist in this dev environment -- same class of gap as Q12 (email) and Q20 (storage), where an external provider was chosen but no credentials exist to actually call it. (2) `Plan.maxActiveJobs`/`maxSeats` exist in the schema, but REQ-SUB-001 only says "plan selection" -- whether *choosing* a plan should also start *enforcing* its limits (blocking job posts/staff invites once hit) isn't stated. | `requirements.md` §3.2 (REQ-SUB/REQ-PAY), Q12, Q20, `Plan`/`Subscription`/`Payment` schema, M13 (Subscriptions) | **Decided:** (1) Same resolution shape as Q12/Q20: build the real `Subscription`/`Payment` schema flow (one `Subscription` per org, updated in place on a plan switch; a `Payment` row recorded per non-zero-amount transaction, `stripeEventId` present for the idempotency the schema already calls for), but stub the actual Stripe API call -- selecting a paid plan is treated as an immediately-"succeeded" payment (a placeholder `stripeEventId`, logged via the server logger) rather than a real Stripe Checkout redirect, until a real Stripe test account is provisioned. (2) Limit enforcement is out of scope for this ticket -- REQ-SUB-001's literal text is "plan selection," not "plan enforcement," and wiring `maxActiveJobs`/`maxSeats` checks into `JobsService`/`OrganizationsService` would be meaningful scope creep into modules this ticket doesn't otherwise touch; revisit as its own ticket once a plan's limits actually need to bind. |
| **Q30** | Starting M14 (Talent Pools): REQ-POOL-001 was also deferred with no main flow (Q27's pattern). Two things need deciding: (1) no permission in `authorization.md` §3's catalog covers talent pools at all -- unlike Q17/Q21/Q23/Q24/Q26 (a missing grant of an *existing* key), this needs a genuinely new one, the same situation as Q27's `onboarding:read`. (2) How does a Recruiter obtain a `candidateId` to tag, given there's no candidate directory to browse independently of applications? | `requirements.md` §3.2 (REQ-POOL), `authorization.md` §3, Q27, M14 (Talent Pools) | **Decided:** (1) Added a new `talentPool:manage` permission, Recruiter-only, covering create/read/tag/untag (the established "manage implies read+write" pattern already used for `pipeline:manage`/`onboarding:manage`). (2) A candidate can only be tagged if `Application.candidateId`+`organizationId` shows they've actually applied to this org (validated server-side, 422 if not -- same "invalid reference in the request body" shape as Q18's `cvId`/`pipelineTemplateId`) -- a Recruiter already has real candidate ids from `GET /jobs/:jobId/applications` responses, so no separate directory/search endpoint is needed, and this incidentally also prevents pooling arbitrary platform users outside the org's own applicant pool. |
| **Q31** | Starting M15 (University Partnerships): REQ-UNI-001 was also deferred with no main flow (Q27's pattern). Its actor list is unusual -- "Super Admin, Company Owner" -- two different roles acting on two different models (`University` the shared catalog, `UniversityPartnership` the org-specific link), and neither `authorization.md` §3's Super Admin nor Company Owner row has any permission covering either. | `requirements.md` §3.2 (REQ-UNI), `authorization.md` §3, `University`/`UniversityPartnership` schema, M15 (University Partnerships) | **Decided:** Split into two genuinely new permissions (same shape as Q27/Q30, not a missing grant of an existing key): `university:manage` (Super Admin only) for the platform-wide catalog (`POST /universities`, mirroring `Plan`'s "seeded/admin-managed, not user-editable" precedent, though `University` is admin-*addable* rather than fully pre-seeded since orgs may request partnerships with any real institution) and `university:partner` (Company Owner only) for the org's own partnership links (`GET`/`POST /organizations/me/partnerships`, `DELETE .../partnerships/:universityId`). `GET /universities` itself is public, no permission at all -- same reasoning as `GET /plans`. |
| **Q32** | Starting M16 (Analytics), the last item in `requirements.md` §3.1: REQ-ANALYTICS-001/002 were also deferred with no main flow (Q27's pattern). Two things need deciding: (1) `authorization.md` §3 has no permission for Recruiter/Hiring Manager/Company Owner covering org-level analytics at all (only Super Admin's `analytics:platform` exists). (2) What exactly counts as "analytics" -- point-in-time counts, historical trends, or derived rate/duration metrics (time-to-hire, stage conversion rates) -- isn't specified, and the schema has no field unambiguously marking *when* a status transition happened (`Application.updatedAt` changes on any update, not specifically a hire). | `requirements.md` §3.2 (REQ-ANALYTICS), `authorization.md` §3/§5, M16 (Analytics) | **Decided:** (1) Added a new `analytics:read` permission to both Recruiter's and Hiring Manager's seeded permissions (Company Owner inherits it automatically via the existing "all org-scoped permissions below" aggregation, the same mechanism Q24's `evaluation:read` used) -- not a missing grant of an existing key, same situation as Q27/Q30/Q31. (2) Scoped to point-in-time funnel counts only (`Job`/`Application`/`Interview`/`Offer` grouped by status) -- no trends or derived metrics, since inventing a "when did this become HIRED" timestamp convention the schema doesn't already define would be exactly the silent business-rule resolution `CLAUDE.md` rule 9 warns against; revisit if/when a real timestamp for stage transitions is added (e.g., promoting `ApplicationStageHistory`'s pattern to cover terminal statuses too). Platform-level analytics (`GET /admin/analytics`, Super Admin, existing `analytics:platform`) is the first endpoint under the `/api/admin/*` namespace `authorization.md` §5 already documents but that no controller had used yet. |
| **Q33** | Building the admin portal's frontend (F6, modeled on the Hirelane prototype's `screen-aover`/`screen-aappr`) surfaced three things the prototype's design assumed but the backend has no way to serve: (1) a "Recent Audit Log" table -- `AuditLog` has been write-only since it was introduced (M5+), no controller ever exposed a read path. (2) A platform-wide user list/count -- no endpoint enumerates `User` records at all (only org-scoped `GET /organizations/me/members`, added in F5). (3) A "Platform Growth" new-orgs-per-month chart -- same missing-timestamp problem as Q32, `Organization.createdAt` alone can't distinguish "submitted" from "approved" trend lines cleanly and nothing else marks a monthly cohort. | `docs/decisions/` (none yet), F6 admin portal, `AuditLog` model, `authorization.md` §5's documented-but-sparse `/api/admin/*` namespace | **Decided:** Left all three out of the built admin portal rather than fabricate data or stub fake rows -- CLAUDE.md rule 9 applies to a missing read path the same as a missing business rule. The admin overview page only shows what `GET /admin/analytics` (Q32) actually returns. Revisit if a real requirement for platform-wide user management or an audit trail UI is added: (1) needs a new `audit:read`-style permission + a paginated `GET /admin/audit-log` endpoint (the model already has everything a reader would need -- actor, action, target, timestamp); (2) needs an equivalent `GET /admin/users` list; (3) is blocked on the same "no status-transition timestamp" gap as Q32 and shouldn't be solved differently here. |
| **Q34** | `requirements.md`'s role table says Company Owner "invites/removes staff", and REQ-RBAC-001 ("Role & permission management within an org") was never given a written main flow (Q27's pattern) -- and the design-canvas `StaffInvite` mockup's Team Members table shows both members and pending invitations in one list with per-row actions, which nothing in the built `/org/team` page (F7) currently supports (invite-only, no way to see pending invites or remove anyone). `user:remove` and `role:assign` have been seeded onto Company Owner since M4 but no controller has ever used either. | `requirements.md` role table + REQ-RBAC-001, `authorization.md` §3 (`user:remove`, `role:assign`), `Invitation` model, F7 (`/org/team`) | **Decided:** Built the "remove staff" and "view/cancel pending invitations" half of REQ-RBAC-001 (not full custom role/permission editing -- `role:assign` still has no concrete UI need yet, since invitation already picks a role and there's no requirement anywhere for *changing* an existing member's role after the fact; left unused, same as before). `GET`/`DELETE organizations/me/invitations[/:id]` (pending, unaccepted, unexpired only -- an accepted or expired invitation has nothing left to cancel) and `DELETE organizations/me/members/:userId` (deletes that user's `UserOrganizationRole` rows for this org only, leaving their account and any other org membership or candidate identity untouched, per Q3), both gated on `user:invite`/`user:remove` respectively. Two safety rules invented here since neither doc specifies them: a Company Owner cannot remove themselves (no self-service "leave the org" flow exists, and it would need its own designed handoff), and cannot remove the organization's last Company Owner (would orphan the org with no one able to manage billing/staff/settings at all). Revisit if a real "transfer ownership" or "leave organization" flow is ever specified. |

## Risks (separate from the above — things to watch, not decisions to make now)

- **Bottleneck risk:** Phases 3-4 (database + auth/RBAC) block nearly everything else. If those slip, the whole team stalls. Mitigation: assign the strongest available developers there and treat it as the first priority, per `team-plan.md` §1.
- **Tenant-isolation regression risk:** with 5 developers of varying experience touching tenant-owned data, a single missed `organizationId` filter is a critical security bug that's easy to introduce and easy to miss in casual review. Mitigation: the `TenantScopedService` base pattern (`multi-tenancy.md` §4) plus the mandatory cross-tenant test case (`testing.md` §2 case 5) as a hard PR gate.
- **Scope risk:** the full feature list (25 functional areas) is large for a semester team of 5. Mitigation: the MVP scope cut in `team-plan.md` §7 exists specifically to protect a working end-to-end demo over broad-but-shallow coverage.
- **Knowledge-silo risk:** module ownership (`team-plan.md` §3) reduces conflicts but risks each developer only understanding their own slice. Mitigation: code review across owners (not just within a module), and this documentation set existing so anyone can ramp on any module.
- **"It works on my machine" risk:** without the Docker/CI setup in Phase 2 being solid early, integration surprises pile up late. Mitigation: prioritize Phase 2 completeness (docker-compose, CI skeleton) before feature work fans out.

## How to Resolve These

Bring this list to a team meeting, decide (override the recommendation if the team disagrees — these are informed defaults, not mandates), and record the decision by editing this file directly (replace "Recommendation" with "**Decided:**" and the chosen answer) — keep the table as the running log of these calls so no one re-litigates a settled question mid-semester.
