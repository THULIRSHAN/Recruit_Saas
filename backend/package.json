{
  "name": "@ats/backend",
  "version": "0.1.0",
  "private": true,
  "description": "NestJS API — placeholder, scaffolded properly in M1.2",
  "scripts": {
    "lint": "echo \"backend lint not configured yet (M1.2)\" && exit 0",
    "typecheck": "echo \"backend typecheck not configured yet (M1.2)\" && exit 0",
    "test": "echo \"backend tests not configured yet (M1.2)\" && exit 0",
    "build": "echo \"backend build not configured yet (M1.2)\" && exit 0"
  }
}
