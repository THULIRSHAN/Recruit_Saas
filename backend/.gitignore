node_modules
# Keep environment variables out of version control
.env

/src/generated/prisma

# Local-disk stub for StorageService (docs/decisions/ADR-004) -- never
# real user data in this repo, and moot once a real provider is wired in.
/storage
