# Copy this file to .env and fill in real values. Never commit .env.
# This file documents variable NAMES and PURPOSE only — no real secrets. See docs/security.md §9.

# --- General ---
NODE_ENV=development

# --- Backend (extended in M1.5 / Prisma setup) ---
# BACKEND_PORT=

# --- Database (extended in M1.4 / Docker Compose + M1.5 / Prisma) ---
# DATABASE_URL=

# --- Frontend (extended in M1.3) ---
# NEXT_PUBLIC_API_URL=
